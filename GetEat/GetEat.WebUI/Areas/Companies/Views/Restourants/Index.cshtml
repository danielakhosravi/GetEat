@model IEnumerable<DomainModel.Entities.Restourant>

@{
    ViewBag.Title = "Index";
}

<div class="logbackground">
    <div class="container">
        <div class="register-form">
            <h3 class="anchorstyle center-content">
                @Html.ActionLink("Add New Restourant", "Create", new { organisationId = ViewBag.OrganisationId })
            </h3>
            <div class="container">
                <table class="table">
                    <thead>
                        <tr>
                            <th class="mobile head">Picture</th>
                            <th class="head">@Html.DisplayNameFor(model => model.Name)</th>
                            <th class="head">@Html.DisplayNameFor(model => model.KitchenId)</th>
                            <th class="mobile head">@Html.DisplayNameFor(model => model.UpdatedDate)</th>
                            <th class="change head">Changes</th>
                        </tr>
                    </thead>
                    <tbody>

                        @foreach (var item in Model)
                        {
                            <tr>
                                <td class="mobile">
                                    <img class="img-company img-rounded" src="~/Content/images/gallery-re.jpg" />
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.Name)
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.Kitchen.Name)
                                </td>
                                <td class="mobile">
                                    @Html.DisplayFor(modelItem => item.UpdatedDate)
                                </td>
                                <td class="change">
                                    @Html.ActionLink("Edit", "Edit", new { restourantId = item.Id }) 
                                    @Html.ActionLink("Details", "Details", new { restourantId = item.Id }) 
                                    @Html.ActionLink("Delete", "Delete", new { restourantId = item.Id })
                                </td>
                            </tr>
                           
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>
@section scripts {
    <script type="text/javascript" src="~/Scripts/geteat/company.js">

    </script>
}
